<form method="post" action="{{ path('app_appointment_category_delete', {'id': appointment_category.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ appointment_category.id) }}">
    <button class="btn">Delete</button>
</form>
