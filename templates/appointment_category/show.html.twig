{% extends 'base.html.twig' %}

{% block title %}AppointmentCategory{% endblock %}

{% block body %}
    <h1>AppointmentCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ appointment_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ appointment_category.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_appointment_category_index') }}">back to list</a>

    <a href="{{ path('app_appointment_category_edit', {'id': appointment_category.id}) }}">edit</a>

    {{ include('appointment_category/_delete_form.html.twig') }}
{% endblock %}
