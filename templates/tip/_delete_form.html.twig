<form method="post" action="{{ path('app_tip_delete', {'id': tip.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tip.id) }}">
    <button class="btn">Delete</button>
</form>
