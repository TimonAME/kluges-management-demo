<form method="post" action="{{ path('app_illness_report_delete', {'id': illness_report.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ illness_report.id) }}">
    <button class="btn">Delete</button>
</form>
