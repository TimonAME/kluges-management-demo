{% extends 'base.html.twig' %}

{% block title %}TeachingMaterial{% endblock %}

{% block body %}
    <h1>TeachingMaterial</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ teaching_material.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ teaching_material.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ teaching_material.description }}</td>
            </tr>
            <tr>
                <th>File_path</th>
                <td>{{ teaching_material.filePath }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_teaching_material_index') }}">back to list</a>

    <a href="{{ path('app_teaching_material_edit', {'id': teaching_material.id}) }}">edit</a>

    {{ include('teaching_material/_delete_form.html.twig') }}
{% endblock %}
