<form method="post" action="{{ path('app_honorarium_delete', {'id': honorarium.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ honorarium.id) }}">
    <button class="btn">Delete</button>
</form>
